# MCP Nextcloud

MCP server for Nextcloud API integration - secure API access from sandboxed agents with authentication layer.

This repository is initialized with an initial commit to establish the main branch.
